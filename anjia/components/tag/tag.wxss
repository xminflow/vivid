.tag {
  display: inline-block;
  padding: 0 8rpx;
  border-radius: 4rpx;
  font-size: 22rpx;
  line-height: 32rpx;
  letter-spacing: 0.02em;
}

.tag--default {
  color: var(--ink-weak);
  background: rgba(23, 22, 20, 0.05);
}

.tag--brand {
  color: var(--brand-ink);
  background: var(--brand);
}
