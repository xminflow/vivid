.amount {
  display: inline-flex;
  align-items: baseline;
  color: var(--ink);
  font-weight: 500;
  letter-spacing: -0.01em;
}

.sym {
  margin-right: 2rpx;
  font-size: 0.72em;
}

.amount--md .num {
  font-size: 32rpx;
}

.amount--sm .num {
  font-size: 26rpx;
}
