# 占位素材，不是内容

这里的图全部来自 Pexels（Pexels License：可商用、免署名），用途只有一个：
让首页内容流的**双列瀑布流**在评审时能看到真实的参差比例——纯色块看不出
图片高度不齐时的观感，而瀑布流的成败恰恰全在这一点上。

⚠️ 它们是**外国样板间的图库照，不是安家立业企业客户的真实案例**。

- 上线前必须整批换成后台配置的真实内容，`anjia/mock/` 也随之删除。
- 不要把这里的图当成视觉基调的参考——它们只验布局，不验调性。
- 12 张主图（比例 0.56 ~ 1.78）+ 8 张头像，共约 676KB，都进包。真实内容
  走接口下发，不会重复这个体积。
