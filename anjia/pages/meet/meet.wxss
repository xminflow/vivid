.page {
  padding: 120rpx 48rpx;
}

.title {
  font-size: 44rpx;
  font-weight: 600;
}

.desc {
  margin-top: 16rpx;
  color: var(--text-weak);
}

.note {
  margin-top: 48rpx;
  color: var(--text-faint);
  font-size: 24rpx;
}
