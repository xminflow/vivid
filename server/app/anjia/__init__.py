"""安家立业。

一个进程装整个矩阵，这个包是安家立业那一半：自己的库、自己的连接池、
自己的微信 appid，路由一律挂在 /api/anjia/ 下。见 docs/adr/0001。

⚠️ 本包**不许 import app.cos**。COS 的配置是模块级全局，读的是安东尼之家的
开发桶，而且是配着的（`cos.configured()` 为真）——同一个进程里 import 进来，
安家立业的文件会安静地写进别人的桶，不报错也不告警。安家立业要用对象存储时，
先把 cos 的配置改成按小程序取，那是一次有自己验收标准的改动。
"""
