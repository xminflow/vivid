# 密钥绝不进镜像，环境变量由部署平台注入
.env
.env.local

# 本地虚拟环境与缓存，镜像里会重新装
.venv/
__pycache__/
*.py[cod]
.pytest_cache/

# 运行时不需要
tests/
README.md
schema.sql
Dockerfile
.dockerignore
# 部署编排。schema.sql / Caddyfile 是直接 scp 到服务器的，不进镜像；
# 这里排掉也顺带保证 deploy/.env 这类真实配置不会被打进构建上下文
deploy/
